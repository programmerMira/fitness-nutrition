models:
  Days:
    training_id: id foreign:Training
    day_number: integer
    name: string:50
    description: string:1000
    videos: json
controllers:
  Days:
    resource: api
seeders: Days

models:
  MenuDays:
    menu_id: id foreign:Menu
    day_number: integer
    name: string:50
    description: string:1000
    content: json
controllers:
  MenuDays:
    resource: api
seeders: MenuDays

